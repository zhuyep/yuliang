{
  "name": "yuliang",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local-first wearable data interpretation and activity planning prototype",
  "repository": {
    "type": "git",
    "url": "https://github.com/zhuyep/yuliang.git"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check:repo": "node scripts/check-repo.mjs",
    "check": "node --check app.js && node --check insights.js && node --check scripts/serve.mjs && npm test && npm run check:repo"
  }
}
